pipeline {
    agent any

    tools{
        maven 'local mvn'
    }

    parameters{
        string(name: 'tomcat_dev', defaultValue: '172.31.0.254', description: 'Staging Server')
        string(name: 'tomcat_prod', defaultValue: '172.31.31.211', description: 'Production Server')
    }
    triggers {
         pollSCM('* * * * *')
    }
    stages{
        stage('Build'){
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo '开始存档...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }

    stage ('Deployments'){
            parallel{
                stage ('Deploy to Staging'){
                    steps {
                        sh "scp -i /home/ubuntu/Jack-AWS-CALIFORNIA.pem **/target/*.war ec2-user@${params.tomcat_dev}:/var/lib/tomcat8/webapps"
                    }
                }

                stage ("Deploy to Production"){
                    steps {
                        sh "scp -i /home/ubuntu/Jack-AWS-CALIFORNIA.pem **/target/*.war ec2-user@${params.tomcat_prod}:/var/lib/tomcat8/webapps"
                    }
                }
            }
        }
    }


}
