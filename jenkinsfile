pipeline {
    agent any
    stages{
        stage('Build'){
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'Start save...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}
